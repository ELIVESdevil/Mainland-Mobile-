<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_black">

    <ImageView
        android:id="@+id/bgSplash"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/bg_splash"
        android:contentDescription="@null" />

    <View
        android:id="@+id/shimmerSplash"
        android:layout_width="140dp"
        android:layout_height="match_parent"
        android:background="@drawable/shimmer_gradient"
        android:rotation="20" />

    <!-- Position and size set at runtime in SplashActivity so it lines up
         with the art's drawn bar regardless of the device's screen shape. -->
    <FrameLayout
        android:id="@+id/progressContainer"
        android:layout_width="0dp"
        android:layout_height="0dp">

        <View
            android:id="@+id/progressTrack"
            android:layout_width="match_parent" android:layout_height="match_parent" />

        <View
            android:id="@+id/progressFill"
            android:layout_width="0dp" android:layout_height="match_parent"
            android:background="@drawable/shape_progress_fill" />
    </FrameLayout>

</FrameLayout>
